
GOOGLE_API_KEY=your_google_api_key

# Path to the SQLite database file (default: clinic.db in project root)
DB_PATH=clinic.db

# Max requests per IP per minute (default: 20)
RATE_LIMIT_PER_MINUTE=20

# Enable in-memory query cache (default: true)
QUERY_CACHE=true
